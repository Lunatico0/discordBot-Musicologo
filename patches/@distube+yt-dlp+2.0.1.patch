diff --git a/node_modules/@distube/yt-dlp/dist/index.js b/node_modules/@distube/yt-dlp/dist/index.js
index b17b126..1787e29 100644
--- a/node_modules/@distube/yt-dlp/dist/index.js
+++ b/node_modules/@distube/yt-dlp/dist/index.js
@@ -74,15 +74,25 @@ var json = /* @__PURE__ */ __name((url, flags, options) => {
   const process2 = (0, import_node_child_process.spawn)(YTDLP_PATH, args(url, flags), options);
   return new Promise((resolve, reject) => {
     let output = "";
+    let errOutput = "";
     process2.stdout?.on("data", (chunk) => {
       output += chunk;
     });
     process2.stderr?.on("data", (chunk) => {
-      output += chunk;
+      errOutput += chunk;
     });
     process2.on("close", (code) => {
-      if (code === 0) resolve(JSON.parse(output));
-      else reject(new Error(output));
+      if (code === 0) {
+        try {
+          resolve(JSON.parse(output));
+        } catch (e) {
+          console.error("[yt-dlp] JSON parse error. stdout:", output.slice(0, 200), "stderr:", errOutput.slice(0, 200));
+          reject(new Error(errOutput || output || "JSON parse failed"));
+        }
+      } else {
+        console.error(`[yt-dlp] Exit code ${code}. stderr: ${errOutput.slice(0, 300)}`);
+        reject(new Error(errOutput || output || `exit code ${code}`));
+      }
     });
     process2.on("error", reject);
   });
@@ -141,13 +151,14 @@ var YtDlpPlugin = class extends import_distube.PlayableExtractorPlugin {
     return true;
   }
   async resolve(url, options) {
+    const cookiesPath = process.env.YTDLP_COOKIES_PATH;
     const info = await json(url, {
       dumpSingleJson: true,
       noWarnings: true,
-      noCallHome: true,
       preferFreeFormats: true,
       skipDownload: true,
-      simulate: true
+      simulate: true,
+      ...(cookiesPath ? { cookies: cookiesPath } : {}),
     }).catch((e2) => {
       throw new import_distube.DisTubeError("YTDLP_ERROR", `${e2.stderr || e2}`);
     });
@@ -171,14 +182,15 @@ var YtDlpPlugin = class extends import_distube.PlayableExtractorPlugin {
     if (!song.url) {
       throw new import_distube.DisTubeError("YTDLP_PLUGIN_INVALID_SONG", "Cannot get stream url from invalid song.");
     }
+    const cookiesPath = process.env.YTDLP_COOKIES_PATH;
     const info = await json(song.url, {
       dumpSingleJson: true,
       noWarnings: true,
-      noCallHome: true,
       preferFreeFormats: true,
       skipDownload: true,
       simulate: true,
-      format: "ba/ba*"
+      format: "ba/ba*",
+      ...(cookiesPath ? { cookies: cookiesPath } : {}),
     }).catch((e2) => {
       throw new import_distube.DisTubeError("YTDLP_ERROR", `${e2.stderr || e2}`);
     });
